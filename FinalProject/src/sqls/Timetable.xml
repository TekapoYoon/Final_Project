<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  
<mapper namespace="Timetable">
	<select id="getTimetableList" resultType="kh.com.a.model.TimetableDto">
		SELECT BUSKINGTIME
		FROM TIMETABLE
		ORDER BY BUSKINGTIME ASC
	</select>
</mapper>
